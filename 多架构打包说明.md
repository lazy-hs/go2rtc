# go2rtc 多架构打包说明

本文档说明如何从源码构建 go2rtc 的不同操作系统和 CPU 架构版本。

## 1. 构建特点

- 项目没有 CGO 依赖，可以在 Windows、Linux 或 macOS 上交叉编译。
- 建议使用 Go 1.25 或更高版本，并尽量使用较新的稳定版 Go。
- `www` 目录中的 HTML、JavaScript 和 JSON 会通过 `go:embed` 自动嵌入可执行文件，无需单独复制前端文件。
- `static` 目录中的上传文件、视频文件以及 `go2rtc.yaml` 不会嵌入程序，部署时需要按实际需求单独复制。
- 使用 `ffmpeg:` 视频源时，目标机器需要额外安装 FFmpeg，或在配置中设置 `ffmpeg.bin`。

项目官方构建参数：

```text
CGO_ENABLED=0
-ldflags "-s -w"
-trimpath
```

参数说明：

- `CGO_ENABLED=0`：关闭 CGO，便于交叉编译并减少系统动态库依赖。
- `-s -w`：移除符号表和调试信息，减小文件体积。
- `-trimpath`：移除编译环境中的本地源码路径。

## 2. 支持的构建目标

| 操作系统    | CPU 架构              | Go 参数                           | 推荐文件名                  |
| ------- | ------------------- | ------------------------------- | ---------------------- |
| Windows | AMD64               | `GOOS=windows GOARCH=amd64`     | `go2rtc_win64.exe`     |
| Windows | 386                 | `GOOS=windows GOARCH=386`       | `go2rtc_win32.exe`     |
| Windows | ARM64               | `GOOS=windows GOARCH=arm64`     | `go2rtc_win_arm64.exe` |
| Linux   | AMD64               | `GOOS=linux GOARCH=amd64`       | `go2rtc_linux_amd64`   |
| Linux   | 386                 | `GOOS=linux GOARCH=386`         | `go2rtc_linux_i386`    |
| Linux   | ARM64               | `GOOS=linux GOARCH=arm64`       | `go2rtc_linux_arm64`   |
| Linux   | ARMv7               | `GOOS=linux GOARCH=arm GOARM=7` | `go2rtc_linux_arm`     |
| Linux   | ARMv6               | `GOOS=linux GOARCH=arm GOARM=6` | `go2rtc_linux_armv6`   |
| Linux   | MIPS Little Endian  | `GOOS=linux GOARCH=mipsle`      | `go2rtc_linux_mipsel`  |
| macOS   | Intel AMD64         | `GOOS=darwin GOARCH=amd64`      | `go2rtc_mac_amd64`     |
| macOS   | Apple Silicon ARM64 | `GOOS=darwin GOARCH=arm64`      | `go2rtc_mac_arm64`     |
| FreeBSD | AMD64               | `GOOS=freebsd GOARCH=amd64`     | `go2rtc_freebsd_amd64` |
| FreeBSD | ARM64               | `GOOS=freebsd GOARCH=arm64`     | `go2rtc_freebsd_arm64` |

## 3. 构建前准备

在项目根目录执行：

```bash
go version
go mod download
go test -count=1 ./internal/api
```

如果当前工程的全量测试包含额外示例、参考前端依赖或平台专用测试，可以先运行与本次修改相关的定向测试，再执行构建。

## 4. Windows PowerShell 构建

以下命令均在工程根目录执行。

### 4.1 Linux AMD64

```powershell
$env:CGO_ENABLED="0"
$env:GOOS="linux"
$env:GOARCH="amd64"
Remove-Item Env:GOARM -ErrorAction SilentlyContinue

go build -ldflags "-s -w" -trimpath -o go2rtc_linux_amd64 .
```

### 4.2 Linux ARM64

```powershell
$env:CGO_ENABLED="0"
$env:GOOS="linux"
$env:GOARCH="arm64"
Remove-Item Env:GOARM -ErrorAction SilentlyContinue

go build -ldflags "-s -w" -trimpath -o go2rtc_linux_arm64 .
```

### 4.3 Linux ARMv7

```powershell
$env:CGO_ENABLED="0"
$env:GOOS="linux"
$env:GOARCH="arm"
$env:GOARM="7"

go build -ldflags "-s -w" -trimpath -o go2rtc_linux_arm .
```

### 4.4 Linux ARMv6

```powershell
$env:CGO_ENABLED="0"
$env:GOOS="linux"
$env:GOARCH="arm"
$env:GOARM="6"

go build -ldflags "-s -w" -trimpath -o go2rtc_linux_armv6 .
```

### 4.5 Windows AMD64

```powershell
$env:CGO_ENABLED="0"
$env:GOOS="windows"
$env:GOARCH="amd64"
Remove-Item Env:GOARM -ErrorAction SilentlyContinue

go build -ldflags "-s -w" -trimpath -o go2rtc_win64.exe .
```

### 4.6 恢复当前机器默认构建环境

完成交叉编译后，可以清理环境变量：

```powershell
Remove-Item Env:CGO_ENABLED -ErrorAction SilentlyContinue
Remove-Item Env:GOOS -ErrorAction SilentlyContinue
Remove-Item Env:GOARCH -ErrorAction SilentlyContinue
Remove-Item Env:GOARM -ErrorAction SilentlyContinue
```

## 5. Linux 或 macOS Bash 构建

### 5.1 Linux AMD64

```bash
CGO_ENABLED=0 GOOS=linux GOARCH=amd64 \
  go build -ldflags "-s -w" -trimpath \
  -o go2rtc_linux_amd64 .
```

### 5.2 Linux ARM64

```bash
CGO_ENABLED=0 GOOS=linux GOARCH=arm64 \
  go build -ldflags "-s -w" -trimpath \
  -o go2rtc_linux_arm64 .
```

### 5.3 Linux ARMv7

```bash
CGO_ENABLED=0 GOOS=linux GOARCH=arm GOARM=7 \
  go build -ldflags "-s -w" -trimpath \
  -o go2rtc_linux_arm .
```

### 5.4 macOS ARM64

```bash
CGO_ENABLED=0 GOOS=darwin GOARCH=arm64 \
  go build -ldflags "-s -w" -trimpath \
  -o go2rtc_mac_arm64 .
```

## 6. 批量构建示例

下面的 Bash 脚本可以连续生成常用 Linux 架构：

```bash
#!/usr/bin/env bash
set -euo pipefail

mkdir -p dist

CGO_ENABLED=0 GOOS=linux GOARCH=amd64 \
  go build -ldflags "-s -w" -trimpath -o dist/go2rtc_linux_amd64 .

CGO_ENABLED=0 GOOS=linux GOARCH=arm64 \
  go build -ldflags "-s -w" -trimpath -o dist/go2rtc_linux_arm64 .

CGO_ENABLED=0 GOOS=linux GOARCH=arm GOARM=7 \
  go build -ldflags "-s -w" -trimpath -o dist/go2rtc_linux_arm .

CGO_ENABLED=0 GOOS=linux GOARCH=arm GOARM=6 \
  go build -ldflags "-s -w" -trimpath -o dist/go2rtc_linux_armv6 .

CGO_ENABLED=0 GOOS=linux GOARCH=386 \
  go build -ldflags "-s -w" -trimpath -o dist/go2rtc_linux_i386 .

CGO_ENABLED=0 GOOS=linux GOARCH=mipsle \
  go build -ldflags "-s -w" -trimpath -o dist/go2rtc_linux_mipsel .
```

## 7. 构建结果校验

### 7.1 查看 Go 构建元数据

该命令可以在编译机器上检查交叉编译结果，无需运行目标程序：

```bash
go version -m go2rtc_linux_amd64
```

输出中应包含：

```text
CGO_ENABLED=0
GOARCH=amd64
GOOS=linux
```

### 7.2 Linux 查看文件类型

```bash
file go2rtc_linux_amd64
```

AMD64 构建结果应显示为 64 位 ELF 可执行文件。

### 7.3 计算 SHA256

Linux 或 macOS：

```bash
sha256sum go2rtc_linux_amd64
```

Windows PowerShell：

```powershell
Get-FileHash .\go2rtc_linux_amd64 -Algorithm SHA256
```

## 8. 压缩与归档

### 8.1 Linux 使用 tar.gz

```bash
tar -czf go2rtc_linux_amd64.tar.gz go2rtc_linux_amd64
```

### 8.2 Windows 使用 ZIP

```powershell
Compress-Archive -Path .\go2rtc_win64.exe -DestinationPath .\go2rtc_win64.zip -Force
```

### 8.3 可选使用 UPX

官方脚本会对 Linux 文件使用 UPX 压缩，但 UPX 不是运行所必需的：

```bash
upx --best --lzma go2rtc_linux_amd64
```

建议先保留一份未压缩文件。部分安全软件、容器扫描器或受限系统可能不接受 UPX 压缩后的程序。

## 9. Linux 部署示例

将程序和配置上传到服务器：

```bash
scp go2rtc_linux_amd64 user@server:/opt/go2rtc/go2rtc
scp go2rtc.yaml user@server:/opt/go2rtc/go2rtc.yaml
```

登录服务器后执行：

```bash
cd /opt/go2rtc
chmod +x go2rtc
./go2rtc -c go2rtc.yaml
```

后台运行可以交给 systemd、supervisor 或容器管理，不建议长期依赖未托管的终端进程。

## 10. 部署注意事项

### 10.1 修改平台相关路径

Windows 配置示例：

```yaml
api:
  upload_dir: "D:/go2rtc/static"
```

部署到 Linux 后需要修改为：

```yaml
api:
  upload_dir: "/opt/go2rtc/static"
```

`streams` 中的本地视频路径也必须改为目标系统真实存在的绝对路径。

### 10.2 安装 FFmpeg

Debian 或 Ubuntu：

```bash
sudo apt update
sudo apt install -y ffmpeg
```

检查安装结果：

```bash
ffmpeg -version
ffprobe -version
```

自定义 FFmpeg 路径：

```yaml
ffmpeg:
  bin: "/opt/ffmpeg/bin/ffmpeg"
```

### 10.3 Linux 执行权限

从 Windows 复制到 Linux 后，通常需要重新添加执行权限：

```bash
chmod +x go2rtc_linux_amd64
```

### 10.4 Go 版本与旧系统兼容性

项目依赖较新的 Go 和第三方库。即使关闭 CGO，较新 Go 版本构建的程序仍可能提高最低 Linux 内核或操作系统版本要求。如果目标系统较旧，应在对应系统或容器中进行实际运行测试。

## 11. 项目已有构建文件

- `scripts/build.sh`：Linux/macOS Shell 多平台构建脚本。
- `scripts/build.cmd`：Windows 批处理多平台构建脚本。
- `.github/workflows/build.yml`：GitHub Actions 多架构构建流程。
- `docker/Dockerfile`：Docker 镜像构建文件。

构建单个架构时，建议直接使用本文档中的 `go build` 命令；需要发布全部架构时，可以参考项目已有脚本和 GitHub Actions。
